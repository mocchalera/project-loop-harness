# v0.5.0 Council Profile dogfood

Date: 2026-07-12

Repository: `project-loop-harness`

Boundary: offline fixture only; no network, paid service, provider code,
credential, production data, or transcript.

## Frozen targets

- `T-0039`: high-risk cross-skill integrity task with two unverified
  assumptions and an approved Work Brief (`E-0160`). Its recorded route is
  Discover (`E-0188`), so Council is eligible.
- `T-0035`: clear-acceptance Direct task with approved Work Brief (`E-0125`).
  Council preparation stopped with `profile_route_mismatch`, confirming that a
  clear task is not silently escalated.

## Reviewed packets

| Packet | Status | Evidence | Decisions | Bytes (request + bundle) | Cost | Replay latency |
|---|---|---:|---:|---:|---:|---:|
| ambiguous/high-risk completion | `completed` | `E-0190` | 0 | 17,180 | 0 | 0.09 s |
| bounded incomplete result | `partial` | `E-0191` | 0 | 17,186 | 0 | 0.12 s |
| intentional safe stop | `budget_exhausted` | `E-0192` | 0 | 17,213 | 0 | 0.10 s |

All three dry-runs reported exactly one Evidence row, Evidence link, event,
outbox record, and finalized directory, with zero Decisions. The request was
15,592 bytes. Secret-pattern scanning of request, artifacts, bundles, plans,
events returned clean. Fixture run metadata reports zero input/output tokens,
zero cost, no network, and no repository content sent.

## Corrections and rework

- First preparation correctly stopped until route Evidence was recorded.
- Attempting Council on the Direct control correctly stopped with
  `profile_route_mismatch`; no override was invented.
- Schema corrections: 0. Human review time for fixture output: 0 minutes;
  deterministic contract and audit review only. Human Decisions: 0.

`pcl finish --emit-packet --dry-run --task T-0039` did not mutate state. A
subsequent `pcl resume --target T-0039` retained all three Council Evidence refs
(`E-0190`–`E-0192`), along with the approved Brief. This proves the existing
handoff path keeps Council artifacts additive rather than rewriting history.

## Recommendation for 0162

**Continue experiment; keep Direct as the default.** The local boundary,
failure handling, provenance, replay, and handoff behavior are ready. Adoption
is not yet justified because this offline cohort cannot measure model quality,
human attention savings, provider cost, or provider latency.
