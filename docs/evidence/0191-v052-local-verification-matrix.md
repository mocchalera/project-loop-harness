# 0191 v0.5.2 local verification matrix

- **Date:** 2026-07-16
- **Goal / Task:** G-0051 / T-0105
- **Scope:** installed-candidate adoption, completion, upgrade compatibility,
  distribution, and MCP verification on one macOS arm64 machine
- **External actions:** none

## Candidate artifacts

The candidate was built from local commit `862e276` without `PYTHONPATH` in
the installed-runtime checks. The package metadata still reports `0.5.1`; this
is not a published v0.5.2 release.

| Artifact | SHA-256 |
| --- | --- |
| `project_loop_harness-0.5.1-py3-none-any.whl` | `26f441cfed7b47e97530cb395afe27f531e5c88488560291b9670da44eb2174b` |
| `project_loop_harness-0.5.1.tar.gz` | `fce271a0c4b9714ff1abd15abfbe018387c34d7d9f53b166d75e4d45367afbbb` |

The build succeeded and repeated the known setuptools license-metadata
deprecation warnings. They are warnings, not artifact failures.

## Verification matrix

| Surface | Result | Evidence |
| --- | --- | --- |
| Python 3.13 clean-wheel adoption | PASS | Dry-run wrote nothing; init, strict doctor, and strict validation were clean. The Direct route reached `COMPLETED_VERIFIED`, closed the Goal against the packet, produced a clean audit check, rendered the Japanese dashboard, and ended at `next: idle`. |
| Node 24 clean-wheel adoption | PASS | Config-ready init selected `npm run test`; a real `node --test` acceptance passed. The Direct route reached `COMPLETED_VERIFIED`, audit clean, and `next: idle`. |
| Isolated pipx install | PASS | The candidate installed under temporary `PIPX_HOME` / `PIPX_BIN_DIR`; init dry-run, init, strict doctor, and strict validation passed. |
| Isolated uv tool install | PASS | The candidate installed under temporary `UV_TOOL_DIR` / `UV_TOOL_BIN_DIR`; init dry-run, init, strict doctor, and strict validation passed. |
| Non-empty repository preservation | PASS | Dry-run file hashes were unchanged, the existing `pcl.yaml` remained byte-for-byte identical after init, and existing AGENTS/CLAUDE content remained present with the managed block appended. |
| Mixed Python + Node repository | PASS with explicit selection | Detection selected Node and its test command. Strict doctor and validation passed. |
| Public v0.5.1 state -> local candidate | PARTIAL | Schema 8 remained consistent with no pending migration. Goal, Task, SQLite/JSONL audit integrity, and the read-only resume packet were preserved. Strict doctor still failed on empty command strings generated by public v0.5.1. |
| Wheel and sdist metadata | PASS | `python -m twine check` passed both artifacts. |
| Extracted sdist contract | PASS | `scripts/verify_sdist_contracts.py` installed the extracted sdist and passed its contract test (`1 passed`). |
| MCP official SDK conformance | PASS | A temporary environment installed the candidate with `mcp-test`; `tests/mcp` completed with `9 passed`, removing the ordinary full-suite skip from this isolated run. |
| Full repository QA | PASS | `1077 passed, 1 skipped in 226.63s`; `ruff check .` and `git diff --check` passed. |

## Upgrade friction reproduced

Public v0.5.1 generated a Node `pcl.yaml` with unused commands represented as
empty strings. The candidate correctly preserves an existing config, so its
`init --dry-run` does not rewrite those values to `null`. As a result:

```text
doctor --strict -> exit 1
config_commands_empty: build, e2e, install, lint, typecheck
```

This does not corrupt durable state: migration status, strict state validation,
audit integrity, Task reads, and resume packets all remained healthy. It is an
upgrade/onboarding friction that needs an explicit repair or upgrade-guide
decision; it is not evidence that external adoption succeeded.

## Environment boundary

- Verified locally on macOS arm64 with Python 3.13.12 and Node 24.8.0.
- Python 3.14.6 is installed but its interpreter failed while creating a venv at
  `ensurepip`; 3.14 is outside the declared Python 3.10-3.13 release matrix.
- Python 3.10-3.12, Linux, and Windows require CI or another machine.
- Temporary artifacts and logs are retained under
  `/tmp/pcl-v052-local-verify.UZMLIC/` for local review.
- No push, tag, release, upload, external message, telemetry, or participant
  record was created.

## Conclusion

The candidate now has installed-artifact proof for the complete Python and Node
golden paths plus both documented tool-install methods. The remaining local
product finding is v0.5.1 empty-command config normalization during upgrade.
External first-use and seven-day reuse remain intentionally unproven.
