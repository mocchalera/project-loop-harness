---
contract_version: master-trace/v0
trace_id: mt-eval-20260715-02-a
source_kind: controlled_dogfood_session
captured_at: 2026-07-15T07:12:00Z
---
# Repository A representative controlled trace

This trace preserves a representative amount of planning and verification context so packet-size comparison is meaningful without exposing an originating chat transcript.
Repository A is project-loop-harness, a local Python CLI whose state changes must pass through public service or CLI boundaries rather than direct SQLite writes.
The authoritative state is SQLite, the JSONL stream is an append-only projection, and the generated dashboard is a human-only rendering rather than machine context.
The v0.5.1 slice keeps schema version 8 and adds no hosted service, telemetry, paid dependency, cloud synchronization, or first-class Trace database table.
The contract-first direction treats master-trace/v0 as copied Evidence and intent-index/v0 as model-produced claims that require deterministic structural and source binding.
An intent index is not semantic truth; even a structurally valid item stays unverified until a consumer opens the copied one-based inclusive line range.
The source_trace object binds Evidence identity, manifest path, original member path, copied stored path, and SHA-256 to one exact Master Trace artifact.
Broken hash, Evidence identity, stored path, and line-range cases must stop before claim selection and must remain visible in the evaluation denominator.
The handoff packet never inlines the full trace or selected source-line text, and trace claims never enter the packet verified array.
Optional trace_claim_refs preserve backward compatibility because a no-index project continues to emit the existing handoff-packet/v1 shape.
Claim selection is deterministic by item identity and source coordinate and never emits a partially truncated item when the byte or item budget is exhausted.
The packet records omitted_sections and explicit trace claim omission reasons so consumers can distinguish absence, invalid binding, ambiguity, and packet budget.
Context check and resume are read-only surfaces; generating either output must not mutate Project Loop state unless a separately documented opt-in records usage.
The first frozen run demonstrated that broken bindings safely stopped but also exposed ambiguous precedence between preparation flags and a later authorization receipt.
The human selected modify, so this second cohort references authorization Evidence E-0422 before freeze and declares that reference authoritative without mutating the cohort later.
The second cohort keeps the original 80 percent valid-resume, 100 percent broken-safe-stop, and zero critical-violation thresholds unchanged.
Failed, assisted, stopped, contaminated, or unexecuted cases remain in their declared denominators and cannot be silently excluded from the aggregate.
Consumers receive only their assigned packet, exact source ranges referenced by valid claim refs, target test code or dependency metadata, and read-only command output.
Consumers do not receive full transcripts, originating-session explanations, other packets, other session results, customer data, media, credentials, or secrets.
The evaluation uses two independent consumer sessions and two owner-controlled repositories; it is controlled dogfood rather than external adoption evidence.
Repository revisions and dirty-worktree limitations are observations, not authorization to edit unrelated code or clean user changes.
The repository A verification surface is the frozen Trace contract fixture test, which checks binding fixtures, deterministic selection, and the evaluation contract.
The consumer must preserve the distinction between packet contract validity and source binding validity: an invalid-binding packet itself may still validate as handoff-packet/v1.
For an invalid-binding packet, the correct useful outcome is a safe stop before source access, not an attempt to repair the packet or infer the hidden next action.
For a valid packet, the consumer opens only its named copied source range, compares the unverified index wording, and then runs the declared repository check.
Assistance means any additional origin explanation or unplanned operator hint; ordinary command output and the exact referenced source range are not assistance.
The result captures packet bytes, trace bytes, selected item count, source access, verification commands, assistance, outcomes, and trust-boundary observations.
Packet efficiency is reported rather than assumed. This trace is intentionally representative and larger than the bounded handoff packet.
The aggregate may recommend continue, modify, or stop, but a human decision remains required before the v0.5.1 release candidate.
No tag, push, GitHub release, PyPI publication, migration, dependency addition, or production action is authorized by this evaluation.
The frozen next step for a valid repository A packet is to run `PYTHONPATH=src pytest -q tests/test_trace_contract_fixtures.py` from project-loop-harness.
Success requires that command to pass after the consumer checks the named copied source lines while keeping every intent-index claim unverified.
The consumer must report that no full transcript or originating-session explanation was received and that no repository or Project Loop state was modified.
