# PCL agent output budget

For an eligible direct, non-interactive verification argv, use `pcl exec -- <argv...>`.
Leave negative, unknown, and already-wrapped argv unchanged. Never rewrite, retry,
upload raw logs, or read PASS diagnostics. See the shared `agent-output-budget` Skill.
